app = "nossobolao-backend"
primary_region = "gru"   # São Paulo

[build]
  dockerfile = "Dockerfile"

[env]
  NODE_ENV = "production"
  API_PORT  = "3000"

[http_service]
  internal_port       = 3000
  force_https         = true
  auto_stop_machines  = false  # mantém máquina sempre ligada (sem cold start)
  auto_start_machines = true
  min_machines_running = 1

  [http_service.concurrency]
    type       = "connections"
    hard_limit = 25
    soft_limit = 20

[[vm]]
  size   = "shared-cpu-1x"
  memory = "512mb"

[checks]
  [checks.health]
    grace_period = "10s"
    interval     = "30s"
    method       = "GET"
    path         = "/api/v1/health"
    port         = 3000
    timeout      = "5s"
    type         = "http"
